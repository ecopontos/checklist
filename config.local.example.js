// Modelo para os valores reais da integração com o GAS.
// Copie este arquivo para "config.local.js" (ignorado pelo git) e
// preencha antes de rodar "npm run build" para que o instalador saia
// com a URL/token já embutidos e a tela Admin venha pré-preenchida.
window.APP_CONFIG = {
    gasUrl: 'https://script.google.com/macros/s/SEU_DEPLOYMENT_ID/exec',
    gasRouteToken: 'SEU_TOKEN_COMPARTILHADO'
};
